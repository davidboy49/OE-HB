BACKEND_URL="http://localhost:3001"

# "Sign in with company SSO" (Keycloak, Authorization Code + PKCE). The button only appears when
# both are set. Use a PUBLIC client whose redirect URI is <site>/api/session/sso/callback.
KEYCLOAK_ISSUER=""
KEYCLOAK_WEB_CLIENT_ID=""
# Only if the site is reached at a different address than the one the server sees (behind a proxy).
# APP_URL="https://oe.example.com"
